meta {
  name: delete query
  type: http
  seq: 32
}

delete {
  url: {{host}}/projects/{{projectId}}/queries/{{queryId}}
  body: text
  auth: inherit
}

headers {
  Content-Type: application/json
}

settings {
  encodeUrl: true
}
